import React from 'react';

const SuggestionMenu = ({ items = [], highlightedIndices = [], selectedIndex, onSelect, query, showHint, hideHint }) => {
  // TODO: move rendering logic from old SuggestionList here
  return <div>SuggestionMenu</div>;
};

export default SuggestionMenu;
